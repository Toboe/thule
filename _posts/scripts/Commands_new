Title: Add another column printing cumulative sum of entries in first column, sum being less than or equal to 100

$ for f in .; do awk 'BEGIN {sum=0;flag=0} {sum=sum+$1; if (flag == 0) { print 
$1"\t"sum > "cumulative.'$f'" } if (sum > 100) flag=1 }' $f; done

Title: print nearest multiples of 100 in a file

$ awk 'BEGIN {count=0;prev=-1} {if(count>0) { if(int($1/100) > int(prev/100)) 
{print $1} } ; prev=$1; count++}' inputFile > rounded
Title: Print starting line numbers of group of lines in a file, which have the same first and second column

$ awk '{pattern=$1$2; seen[pattern]++; if (seen[pattern] == 1) print NR}' 
inputFile
Title: Touch a file using the timestamp in the file name.

$ tstouch() { [[ $1 =~ $2 ]] && touch -t ${BASH_REMATCH[1]} $1; }

tstouch takes two arguments: a filename, and an extended regular expression 
with the parenthesized section matching a timestamp of the form YYYYMMDDhhmm or
YYYYMMDDhhmm.ss.
